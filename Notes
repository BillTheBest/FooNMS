====== Notes: ======
  * password in users in DB is kept in SHA1 format.

====== Ideas: ======
  * Suggestion to keep central records of remote access data for plugins, so that it isn't replicated. [TheSkorm]
  * Maybe we should base the web application on top of a CMS such as Drupal, so that we don't have to write a heap of the user and presentation code ourselves? [JackScott]

===== Debian Deps: =====
  * python-pysnmp2
  * python-mysqldb
  * python-sqlalchemy

